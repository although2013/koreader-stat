local _ = require("gettext")

return {
    name = "readingstat",
    fullname = _("Reading stat uploader"),
    description = _([[Uploads your statistics.sqlite3 to a self-hosted server so you can view reading stats on a web dashboard.]]),
}
